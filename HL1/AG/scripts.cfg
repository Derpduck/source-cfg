alias "+quickbow" "+attack2;wait;+attack;wait;-attack;wait;-attack2"
alias "-quickbow" "wait;lastinv;lastinv"

alias WeaponNext "invnext;+attack;wait;-attack;normal" 
alias WeaponPrev "invprev;+attack;wait;-attack;normal"

alias +dd "-duck;wait;+duck;wait;-duck;wait;+duck"
alias -dd -duck

alias +usespam "alias _special usespam;usespam"
alias -usespam "alias _special"
alias usespam "+use;wait;-use;wait;special"

alias quse "-use;wait;+use;wait;-use"